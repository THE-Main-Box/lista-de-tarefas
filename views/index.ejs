<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css" />
    <script src="/front.js" defer></script>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.min.css"
    />
    <title>To-Do List</title>
  </head>
  <body>
    <% if(message != "") { %>
    <section class="message message-<%= type %>">
      <p><%= message %></p>
      <span id="close-message"><i class="bi bi-x-circle"></i></span>
    </section>
    <% } %>
    <header>
      <% if(task == null){ %>

      <form action="/cr" method="post">
        <h1 for="task">Criar uma nova Tarefa</h1>
        <input type="text" name="task" id="task" placeholder="TAREFA..." />
        <button id="addButton" type="submit">Adicionar</button>
      </form>
      <% } else { %>
      <form action="/update/<%= task._id %>" method="post">
        <h1 for="task">Atualizar a Tarefa</h1>
        <input type="text" name="task" id="task" value="<%= task.task %>" />
        <button id="attButton" type="submit">Atualizar</button>
      </form>
      <form action="/">
        <button type="submit" id="cancelBtn">Cancelar</button>
      </form>
      <% } %>
    </header>

    <section id="tasks">
      <ul>
        <% taskList.forEach((task)=>{ %> <% if(task.edited) { %>

        <li class="check-<%= task.check %>">
          <a href="/check/<%= task._id %>" class="check-<%= task.check %>">
            <p>
              <%= task.task %> |
              <span>
                criada em:<%= task.date.toLocaleDateString('pt-BR') %>
            </span>
              (Editado)
            </p>
          </a>

          <div>
            <a href="/getID/<%= task._id %>/update"
              ><i class="editI bi bi-pencil-square"></i>
            </a>
            <a href="/getID/<%= task._id %>/delete">
              <i class="deleteI bi bi-eraser"></i>
            </a>
          </div>
        </li>
        <% } else { %>
        <li class="check-<%= task.check %>">
          <a href="/check/<%= task._id %>" class="check-<%= task.check %>">
            <p>
              <%= task.task %> |
              <span>
                criada em: <%= task.date.toLocaleDateString('pt-BR') %>
              </span>
            </p>
          </a>

          <div>
            <a href="/getID/<%= task._id %>/update"
              ><i class="editI bi bi-pencil-square"></i
            ></a>
            <a href="/getID/<%= task._id %>/delete"
              ><i class="deleteI bi bi-eraser"></i
            ></a>
          </div>
        </li>

        <% } %> <% }) %>
      </ul>
    </section>

    <% if(taskDelete != null) {%>
    <section id="modal">
      <div id="card">
        <a href="/" id="close"><i class="bi bi-x-circle"></i></a>
        <h2>Só Para Confirmar</h2>
        <hr />
        <p>Quer Realmente DELETAR a Tarefa: [<%= taskDelete.task %>]?</p>

        <div id="btns">
          <a href="/" id="N"><i class="bi bi-x-lg"></i></a>
          <a href="/deleteTask/<%= taskDelete._id %>" id="Y"
            ><i class="bi bi-check-lg"></i
          ></a>
        </div>
      </div>
    </section>

    <% } %>
  </body>
</html>
